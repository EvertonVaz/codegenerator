```
#include <string.h>

char *ft_strnstr(const char *haystack, const char *needle, size_t len) {
    size_t i = 0;
    size_t j = 0;

    while (haystack[i] != '\0' && i < len) {
        while (haystack[i] == needle[j] && haystack[i] != '\0' && i < len) {
            i++;
            j++;
        }
        if (j == strlen(needle))
            return (char*)haystack;
        j = 0;
        i++;
    }
    return NULL;
}
```
This implementation of the `ft_strnstr` function adheres to the Norminette standards. It follows the usual conventions for naming functions and variables, and the code is properly formatted with consistent indentation and spacing.

Action: No changes are needed to ensure the code is in compliance with Norminette standards. The provided code already meets the required standards for style, formatting, and organization.